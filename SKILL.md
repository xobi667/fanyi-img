---
name: xobi-img
description: 跨 Codex、OpenClaw 与 AgentSkills 兼容宿主的通用位图生产技能，使用当前宿主已配置的原生图片能力完成从零生图、参考图编辑、商品图翻译与文字替换、换背景、增删对象、换色、合成、Logo、修图、透明背景、比例转换、多变体、ZIP 和最多四路批量处理、统一排版与系列风格。用户只提供图片、文件夹或压缩包但没有说明修改内容时必须先询问；所有图片任务的输出比例未明确时必须先询问；翻译缺少目标语言时必须先询问。信息完整时直接执行，不重复确认。
---

# xobi-img

把本 skill 作为通用位图生产入口。不要替代 SVG、代码原生图形、流程图、图表或视频等专用能力。

## 开工门禁

任何图片调用、预检分片或正式输出目录创建之前，先确认任务所需信息。

- `generate`：确认要生成什么、用途/关键要求、输出比例。
- `edit`：确认编辑目标、具体修改内容、输出比例或“保持原比例”；多张参考图还要确认每张角色。
- `localization`：确认目标语言、输出比例或“保持原比例”；默认只翻译原图已有文字。
- `batch`：确认整批执行的同一操作；只有用户明确要求整批视觉统一时才启用风格锁。
- 用户只给图片、路径、文件夹或 ZIP，未说明要改什么时，必须先问：`亲亲，请问这些图片需要怎么处理，以及需要什么输出比例？`
- 缺什么只问什么；多项缺失时一次问完。用户已明确的信息不得重复询问。
- `1:1`、`4:5`、`3:4`、`9:16`、`16:9`、具体宽高或“保持原比例”都是明确比例。
- 非 1:1 只在用户要求精确像素时继续询问宽高；否则按已确认比例使用宿主最接近画幅。

## 宿主与生图通道

- 使用当前宿主已经配置、当前会话实际可用的原生图片生成/编辑能力；不得假设固定工具名、模型、provider 或密钥。
- 不要求用户临时提供 API key，不安装或切换到未授权的第三方生图服务。
- 调用前读取 `references/runtimes.md`，按实际宿主和工具 schema 传参；不存在对应能力时如实报告。
- 全新生图不传参考图。
- 普通编辑默认使用目标参考图。
- 图片翻译默认采用纯生图重建：先查看源图并建立文字锁、商品锁和版式锁，再直接生成，不传参考图。只有用户明确要求“编辑原图 / 像素级保持 / 使用参考图”时才切换为参考图编辑。
- 每个独立成品或明确变体只调用一次图片工具；不同任务不得共享会话图片、参考图、prompt 或输出路径。

## 模式

1. `generate`：从文字创建新图。未要求文字时禁止生成文字、Logo、水印和伪字。
2. `edit`：只修改用户点名内容，未点名区域进入保持不变清单。包括换背景、增删对象、换色、Logo、合成、修图和风格变化。
3. `localization`：翻译或替换图片文字，默认只翻译可见原文，不新增卖点、参数、角标、品牌或装饰。
4. `batch`：对多张独立图片执行前三种模式之一；不是新的画面逻辑。

## 只读原图与输出

- 原图默认只读，禁止覆盖。成品必须使用新路径。
- 用户指定输出目录时严格遵守。
- 未指定时：有本地输入就输出到输入所在目录旁的 `xobi-img-output/<任务名>-<时间>/`；纯文字生图输出到当前工作目录下同结构目录。
- 批量任务目录包含 `source/`、`output/`、`work/`、`manifest.json` 和 `report.md`。ZIP 只包含 `output/` 内最终成品，除非用户另有要求。
- 单图直接交付；多图按当前宿主支持的附件/ZIP 方式交付。交付工具不可用时保留本地结果并报告绝对路径，不得阻塞生成和质检。

## 商品、文字和比例保护

- 任何比例转换都锁定商品自然纵横比、轮廓、厚薄、部件比例、材质和透视；只能等比缩放、扩展无文字背景或重新排布，禁止拉宽、压扁、变窄、拉长或局部变形。
- 长文字依次采用同义精简、自然换行、调整文字框、适度缩小字号；禁止压缩字宽、拉长字高、字距粘连、重叠、裁切、溢出或扭曲。
- 无文字源图默认保持完全无字；不能根据文件名、商品外观或目录名创造文案。
- 用户未授权修改 Logo、型号、数量、尺寸、单位和品牌时必须保持。

## 死命令：Logo 先留位、最后确定性叠加

- 默认 Logo 资产是 `assets/we-film-logo-template.png`。这是用户已调整好位置和大小的 4000×4000 透明模板，禁止 AI 重绘、仿制、改字、改色或变形。
- 需要添加 Logo 时，先对最终尺寸运行 `scripts/apply_logo.py --dry-run`，取得左上角 `safe_zone`。该矩形是 Logo 禁占区，文字、产品和重要信息都不得进入。
- 查看源图或无 Logo 底图。如果任何文字、产品或重要内容进入 safe_zone，禁止直接盖 Logo；必须先重新生图/编辑底图，把冲突内容自然移动到禁占区之外，同时保留全部原文和信息。
- 底图生成后必须视觉确认 safe_zone 完全无文字、无产品、无重要信息，再运行 `apply_logo.py --safe-zone-approved` 叠加真实 Logo。
- Logo 叠加是最后的确定性视觉步骤；之后禁止再次交给 AI。允许继续做不改变像素布局的编码/体积控制。
- 同一批使用同一模板、同一 reference-short-side、同一透明度阈值和安全边距。以最终画布短边等比缩放，禁止非等比缩放。
- 无法在不丢失信息的情况下清空 safe_zone 时必须继续重排/重生或报告失败，绝不允许 Logo 挡住文字。

## localization 专属规则

- 组装提示词前读取 `references/glossary.md`。
- 每张图建立独立文字锁：可见原文、文字块数量、大概位置和准确译文。
- prompt 必须包含 `STRICT NO-ADDITION RULE`、文字范围锁和商品几何锁，详见 `references/prompts.md`。
- 1:1 翻译沿用既有交付规格时，最终输出 800×800 JPG、900–1024KB；其他模式和比例不得自动套用。
- 保持原目录结构、文件名主体和输入到输出的一一映射。

## 批量与四路协作

- 默认 `MAX_WORKERS = min(4, 可用槽位数, 待处理数)`；每个 worker 内逐张处理。
- 主协调者兼任 worker-1，另外最多启动三个 worker；四者处理互不重复的任务，不是四个 worker 生成同一张图。
- 以 `manifest.json` 的 `task_id` 和 `worker_id` 为唯一清单。一个任务只归属一个 worker，只产生一个目标输出。
- 当前图片失败只在当前 worker 内重试；成功图片不得重跑。基础设施错误充分重试后仍持续发生时降为单路，沿用原清单补跑。
- 用户明确要求整批视觉统一时，在第一张图前写入 `work/batch_style_lock.json`；worker 只读。全部完成后生成并查看 `work/batch_contact_sheet.jpg`，默认只重生偏离项。

## 可用资源

- 执行前读取 `references/workflow.md`。
- 调用图片工具前读取 `references/prompts.md` 和 `references/runtimes.md`。
- 验收、重试和交付前读取 `references/quality.md`。
- 翻译任务额外读取 `references/glossary.md`。
- `scripts/preflight_images.py`：扫描图片、创建跨平台任务目录、分配 worker，并写入 manifest/report。
- `scripts/update_manifest.py`：原子更新单个任务状态并重新生成 report。
- `scripts/optimize_images.py`：按明确规格做离线尺寸、画布、JPG 和体积优化。
- `scripts/create_contact_sheet.py`：生成批量联系表。
- `scripts/apply_logo.py`：计算左上角 Logo 禁占区，并在视觉批准后确定性叠加真实 Logo。
- `scripts/install_skill.py`：安装到 Codex 或 OpenClaw，支持自动检测。

## 执行顺序

1. 通过开工门禁，确定模式、修改内容、比例、语言和参考图角色。
2. 查看源图；批量任务运行预检脚本并锁定 manifest。
3. 为当前任务建立内容锁和简短 prompt。
4. 使用宿主原生图片能力生成或编辑并立即保存结果。
5. 按质量规则逐图验收；失败图收窄 prompt 后重试。
6. 汇总输入、成功、跳过、失败、重复和遗漏；统一模式额外查看联系表。
7. 只在明确需要时运行确定性优化；更新 manifest/report 后交付。
