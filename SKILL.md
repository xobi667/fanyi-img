---
name: "fanyi"
description: "Codex 内置生图统一入口：新图、改图、翻译、ZIP 批量与风格一致性。"
---

# Fanyi：统一图片生产

本 skill 是工作空间唯一的通用位图生产入口。它覆盖生图、改图、图片翻译和批量交付，但不替代 SVG、代码原生图形、流程图或表情包等专用 skill。

## 硬规则

- 只使用当前 Codex 会话自带的内置生图/编辑能力。
- 禁止调用 OpenClaw 外部 `image_generate` provider、Gemini、Nano Banana、OpenAI SDK/CLI、第三方网关或任何要求 API key 的生图链路。
- 禁止要求用户配置、提供或补充生图 key。内置能力失败时，只能重试内置能力或如实报告，不能切换外部服务。
- 原图默认只读；成品使用新文件名，禁止覆盖用户原图。
- 用户未明确要求改尺寸时，编辑任务保持原尺寸或原比例；普通新图使用内置默认尺寸即可。
- 未要求文字时，禁止自行加入文字、Logo、水印、角标和品牌标识。
- 多图任务先标记每张图片的角色，再生成；不能把“编辑目标”“风格参考”“Logo/素材”混为一谈。

## 先路由任务

每次先判断主模式，再决定是否叠加批量执行：

1. `generate`：从文字创建全新图片。
2. `edit`：修改现有图片，包括换背景、增删对象、换色、合成、加 Logo、修图和风格变化。
3. `localization`：翻译或替换图片内文字，同时锁定商品、版式和未指定区域。
4. `batch`：对多张独立图片重复执行 generate、edit 或 localization。批量是执行方式，不是第四种画面逻辑。

只有 localization 使用目标语言、逐字翻译、翻译目录、800×800 和体积控制等专属规则。普通生图和改图不得被这些规则污染。

## 用户明确要求整批视觉统一时

只有用户明确要求“整批统一排版、做成同一套视觉、保持系列风格”等视觉统一目标时，才启用 `BATCH_STYLE_LOCK`。仅要求产品、文字含义、颜色、尺寸或内容一致时不得误触发；语义不清时先确认。

- 主协调者在第一张图生成前建立并原子写入 `<任务目录>/batch_style_lock.json`，至少记录版本、画布、字体视觉、标题/正文层级、对齐、文字区域、边距、间距、色板、标签/图标风格、商品自然视觉尺度、参考来源和 `content_isolation=true`。
- 风格锁只共享抽象视觉规范，绝不保存或共享任何单图译文、参数、商品内容、构图细节或会话图片。
- 每张图仍使用独立的 `task_id`、文字锁、商品几何锁、prompt 和参考图集合；worker 只读同一风格锁，修订只能由主协调者递增版本。
- 全部生成后必须运行 `scripts/create_contact_sheet.py` 并查看整批联系表。默认只重生偏离风格锁的图片；只有共享基准错误或用户明确要求全部重做时才重做整批。

## 输入与参考图

- 信息足够时直接执行，不重复确认。
- 全新生图：不要传任何参考图字段。
- 编辑本地图片：先查看图片，再把本地路径作为 `referenced_image_paths` 传入内置生图工具。
- 目标图只存在于最近会话且没有本地路径：只使用覆盖所有目标图所需的最小 `num_last_images_to_include`。
- `referenced_image_paths` 和 `num_last_images_to_include` 绝对不能同时使用。
- 所需图片缺失、附件不可访问或无法覆盖全部参考图时，让用户重新上传缺失图片。
- 多图先建立角色表：编辑目标、风格参考、Logo、合成素材、版式参考；提示词中逐一写清。

## 执行顺序

1. 按模式读取 `references/workflow.md`。
2. localization 额外读取 `references/glossary.md`；所有模式在调用生图前读取 `references/prompts.md`。
3. 本地图编辑先查看源图，记录尺寸、比例、文字、主体、布局和必须保持不变的区域。
4. 组织提示词：目标变化 + 必须保持不变的内容 + 输出约束。
5. 使用 Codex 内置生图能力生成或编辑。每个独立资产、每个明确变体分别调用一次，避免串图。
6. 按 `references/quality.md` 验收。明显失败时可用更明确的同模式提示重试；不得切换外部模型。
7. 只有用户明确要求确定尺寸/格式/体积，或 localization 的既定交付规则要求时，才运行本地确定性后处理脚本。

## 本地脚本的边界

- `scripts/preflight_fanyi.py` 只负责扫描、尺寸核对、任务分配和已有成品检查，不做 OCR，也不生图。
- `scripts/final_optimize_images.py` 只负责明确要求下的缩放、白底画布、JPG 和体积控制，不做生图或语义编辑。
- `scripts/create_contact_sheet.py` 只负责从最终图片生成确定性联系表，用于用户明确要求统一时的横向质检。
- 普通生图、透明背景、非 1:1 图片和需要保留透明通道的图片，默认不得运行 `final_optimize_images.py`。
- localization 已有成品不合格且需要重做优化时，显式使用 `--overwrite`；不能只报告失败就结束。
- 三个脚本都是本地离线辅助，不得新增网络、provider 或 key 依赖。

## 输出与交付

- 有本地文件时，统一放入 `xobi/图片生成/YY/MM/DD/任务名-HHMMSS/`；翻译任务可在其中按目标语言建子目录。
- 输出名必须可追溯到原图；批量任务保留原图到成品的映射和成功/失败计数。
- 批量与 ZIP 任务必须实际写入 `manifest.json` 和 `report.md`，不能只把它们当成建议。
- 输入为 ZIP 且主要内容是图片时，默认解压全部有效图片、逐图隔离处理、保留原文件名主体、核对输入输出数量，再重新打包交付。
- 用户要求多组创意或多个变体时，每个变体独立调用并记录 prompt 到成品的映射；本地结果较多且需要浏览时，可生成不依赖外部服务的 `index.html` 缩略图画廊。
- 单张图片直接交付；多张图片在本地文件齐全时可打 ZIP，并保留单图映射。
- 飞书中的 2 张或以上、文件夹或 ZIP 任务默认交付只含最终成品的 ZIP；可额外发送 1 张代表性预览图，但禁止把 ZIP 内全部图片再重复发送。
- `manifest.json`、`report.md`、`batch_contact_sheet.jpg`、原图和中间图都是任务/质检工件，默认不放进交付 ZIP，除非用户明确要求。
- 飞书当前会话只交付一次，禁止工具消息和普通回复重复发送同一结果。
- 单图在最终一次内置生图调用后，不再追加下载说明、图片复述或追问，让生成结果本身作为交付。批量/ZIP 任务则按任务清单完成全部独立调用后统一打包交付，禁止逐张重复发送。

## 相关参考

- `references/workflow.md`：四种模式、尺寸、批量和目录。
- `references/prompts.md`：通用生图、编辑、翻译、合成和透明背景提示词约束。
- `references/quality.md`：单图/批量验收、重试和报告。
- `references/glossary.md`：翻译语言、排版、单位和商品词。
