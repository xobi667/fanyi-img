interface:
  display_name: "xobi-img"
  short_description: "通用生图、fanyi 参考翻译、主图与 Logo 批处理"
  default_prompt: "使用 $xobi-img 处理图片。普通生成和编辑沿用当前纯生图规则；翻译时把当前源图作为唯一参考，只替换已有文字，不得重设计或添加内容。只有我明确要求制作、重做、重新设计或整体优化整张主图时进入创意主图路线。只有我明确要求添加 Logo 且确认本次 Logo 资产时才启用 Logo 流程；无真实遮挡不调用图片模型，直接叠加，有遮挡才局部参考重排后叠加。"
