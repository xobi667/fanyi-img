interface:
  display_name: "xobi-img"
  short_description: "无参考纯生图生成、翻译、编辑与 Logo 四路批处理"
  default_prompt: "使用 $xobi-img 在确认目标、比例和翻译语言后默认无参考纯生图；仅用户明确添加并提供 Logo 时启用 Logo 例外：无遮挡不调用模型直接叠加，有遮挡才局部参考重排后叠加。"
